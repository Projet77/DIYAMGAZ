// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id          Int      @id @default(autoincrement())
  title       String
  description String?
  price       Float
  quantity    Int      @default(0)
  category    String
  photos      String?  // Store as JSON string or string array
  views       Int      @default(0) // New field for tracking popularity
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  inventoryLogs InventoryLog[] // Relation for tracking stock changes
}

model InventoryLog {
  id        Int      @id @default(autoincrement())
  productId Int
  product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  quantity  Int      // The new absolute quantity of the product at this time
  createdAt DateTime @default(now())
}

model Order {
  id           Int      @id @default(autoincrement())
  totalAmount  Float
  deliveryZone String?
  deliveryFee  Float
  status       String   @default("PENDING")
  createdAt    DateTime @default(now())
}

enum Role {
  CLIENT
  B2B
  ADMIN
}

model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  name      String?
  role      Role     @default(CLIENT)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
